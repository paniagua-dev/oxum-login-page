export enum TOKEN {
    name = 'token',
    socialUser = 'socialUser',
}

export enum OAUTHMETHOD {
    SOCIAL = 'Social', // Google Oauth, Facebook...
    FIREBASE = 'Firebase',
    DEFAULT = 'Default', // E-mail and password
}
